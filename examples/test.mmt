fn create_plot() {
  let x = zeros(1, 100);
  let y = zeros(1, 100);

  let xc = 0;

  for(let i=0;i<100;i++) {
    x[i] = xc;
    y[i] = sin(x[i]);
    xc += 0.1;
  }

  plot(x, y);

  return 0;
}

create_plot();